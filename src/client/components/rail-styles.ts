// Shared label styles for the context rail's section headers — used by the
// rail sections (TurnInspector, TokenChart) so the typographic register stays
// one definition.
export const RAIL_LABEL = 'font-mono text-[11px] tracking-[0.18em] uppercase text-fg-3 mb-1';
export const RAIL_SUB = 'font-mono text-[10.5px] tracking-[0.16em] uppercase text-fg-3 mt-1.5';
