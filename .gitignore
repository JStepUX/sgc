# Dependencies
node_modules/

# Build output
dist/
build/
.vite/

# TypeScript / test artifacts
*.tsbuildinfo
coverage/

# Playwright MCP — screenshots and traces from agent visual checks
.playwright-mcp/
sgc-initial-render.png

# Environment variables
.env
.env.local
.env.*.local

# Logs
logs/
*.log
npm-debug.log*

# OS artifacts
.DS_Store
Thumbs.db

# Editor directories
.vscode/
.idea/

# Claude Code local settings (permissions, local env) — never commit
.claude/settings.local.json

# Claude Code session-local state (pre-commit-qa marker, etc.)
.claude/state/

# Claude Code ScheduleWakeup runtime lock — session/pid/timestamp
.claude/scheduled_tasks.lock

# Claude Code agent worktrees — isolated branches the Agent tool creates
.claude/worktrees/

# Archived/superseded specs — kept locally, out of version control
docs/ignored/
